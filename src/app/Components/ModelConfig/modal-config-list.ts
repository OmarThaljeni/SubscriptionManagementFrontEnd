import { NgbModalOptions } from "@ng-bootstrap/ng-bootstrap";

export const ModalConfig: NgbModalOptions = {
    size: 'xl',
    backdrop: 'static',
    keyboard: true,
    scrollable:true,
    centered : true
}